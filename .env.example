# Database Configuration
POSTGRES_USER=gapuser
POSTGRES_PASSWORD=gappassword
POSTGRES_DB=gapdb
POSTGRES_PORT=5433

# Prisma Database URL
# For local development: เชื่อมต่อจาก host machine ไปยัง Docker database
# DATABASE_URL="postgresql://gapuser:gappassword@localhost:5433/gapdb?schema=public"

# For Docker Compose (internal network): ใช้เมื่อ app รันใน Docker container
DATABASE_URL="postgresql://gapuser:gappassword@postgres:5432/gapdb?schema=public"

# For Azure PostgreSQL (production)
# DATABASE_URL="postgresql://gapuser:password@gap-is-wa-db.postgres.database.azure.com:5432/postgres?sslmode=require"

# NextAuth Configuration (สำหรับ Authentication)
NEXTAUTH_SECRET=your-nextauth-secret-here-min-32-characters
NEXTAUTH_URL=http://localhost:3000

# For Azure deployment
# NEXTAUTH_URL=https://gap-is-wa-app.[unique-id].southeastasia.azurecontainerapps.io

# Uploadthing Configuration
UPLOADTHING_TOKEN='your-uploadthing-token-here'

# Other Environment Variables
NODE_ENV=development

# Default Admin User for Seeding
DEFAULT_ADMIN_EMAIL="your-admin-email@example.com"
DEFAULT_ADMIN_PASSWORD="your-admin-password"
DEFAULT_ADMIN_NAME="Your Admin Name"

# Azure Specific (ถ้าใช้ Azure services)
# POSTGRES_SSL=true
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string

# Default password for generated users
DEFAULT_PASSWORD=your-default-password-here

# Playwright Testing Environment
E2E_BASE_URL="your-base-url-for-testing"
E2E_TEST_USER_EMAIL="your-test-email@example.com"
E2E_TEST_USER_PASSWORD="your-test-password"
E2E_TEST_USER_NO_FARMS_EMAIL="your-no-farms-email@example.com"
E2E_TEST_USER_NO_FARMS_PASSWORD="your-no-farms-password"